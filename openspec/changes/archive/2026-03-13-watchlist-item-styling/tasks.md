## 1. Integration

- [x] 1.1 Update `New()` in `internal/ui/app.go` to use `browserItemDelegate` instead of `list.NewDefaultDelegate()`
- [x] 1.2 Update mouse click detection in main `Update()` to use itemHeight=3

## 2. Testing

- [x] 2.1 Verify watchlist items display with dark grey background
- [x] 2.2 Verify selected item displays with lighter background
- [x] 2.3 Verify mouse clicks select correct items with new height
