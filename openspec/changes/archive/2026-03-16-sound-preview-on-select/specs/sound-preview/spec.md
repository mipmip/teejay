## ADDED Requirements

### Requirement: Sound preview on selection
The system SHALL play the selected sound type as a preview when the user cycles through sound types in the pane configuration menu.

#### Scenario: Preview sound when cycling sound type
- **WHEN** user presses Enter on the Sound Type option in pane configuration
- **THEN** the sound type cycles to the next option
- **AND** the newly selected sound plays immediately as a preview
