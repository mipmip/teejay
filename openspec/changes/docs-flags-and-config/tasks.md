## 1. README Update

- [x] 1.1 Fix branding: change "Terminal Junky" to "Terminal Jockey" in README title and description
- [x] 1.2 Update Features list to include multi-column layout, quick-answer popup, recency colors, activity sort, picker mode
- [x] 1.3 Add CLI Flags section after Usage with all current flags (General, Alerts, Display, Mode)
- [x] 1.4 Add Keybindings section with table of all TUI keys (navigate, enter, space, v, o, a, s, c, d, e, q, etc.)
- [x] 1.5 Update Configuration Options table to include display section options (recency_color, sort_by_activity, layout_mode, picker_mode)

## 2. Config Example

- [x] 2.1 Add `display` section to config.example.yaml with all display options and comments

## 3. Documentation Guardrail

- [x] 3.1 Add a documentation sync rule to the OpenSpec project context (openspec/config.yaml) reminding to update README, config.example.yaml, and printHelp() when adding flags/config/keybindings
